{
  "name": "sos-property-inventory",
  "version": "1.0.0",
  "description": "SOS Property inventory management app",
  "private": true,
  "scripts": {
    "sync": "cp index.html manifest.json favicon.svg sw.js www/ && cp -r icons/. www/icons/ && cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.1"
  }
}
